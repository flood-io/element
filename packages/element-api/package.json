{
	"name": "@flood/element-api",
	"version": "1.0.10-beta.7",
	"description": "API definitions for Element",
	"types": "./dist/api.ts.d",
	"scripts": {
		"prebuild": "rm -rf dist",
		"build": "yarn ncc build -e @types/puppeteer -e assert -e comment-parser -e csv -e cuid -e d3-array -e diff -e faker -e knuth-shuffle -e prettier -e puppeteer -e recast -e sanitize-filename -e source-map -e fs-extra -e strftime -e typescript -e term-img -e vm2 -e winston -o dist ./api.ts"
	},
	"devDependencies": {
		"@zeit/ncc": "^0.20.4",
		"typescript": "^3.6.2"
	}
}
