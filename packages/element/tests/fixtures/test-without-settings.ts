import { step, TestSettings } from '@flood/chrome'

export const settings: TestSettings = {}

/**
 * Empty test for evaluating defaults
 *
 * Use this in the test environment.
 */
export default () => {}
